# DemosJarco Search API

- **OpenAPI Version:** `3.1.0`
- **API Version:** `PRODUCTION`

## Servers

- **URL:** 

## Operations

### GET /search

- **Method:** `GET`
- **Path:** `/search`

#### Responses

##### Status: 200 Return results in the requested format.

###### Content-Type: application/json

- **`answers` (required)**

  `array` — Direct answers returned by engines

  **Items:**

  - **`engine`**

    `string` — Engine providing the answer

  - **`parsed_url`**

    `array` — Parsed URL parts

    **Items:**

    `string`

  - **`url`**

    `string`, format: `uri` — A link related to this \*result\*

- **`corrections` (required)**

  `array` — Query corrections suggested by engines

  **Items:**

  `string`

- **`infoboxes` (required)**

  `array` — Infoboxes compiled from engines

  **Items:**

  **All of:**

  - **`category`**

    `string`

  - **`content`**

    `string`

  - **`engine`**

    `string`

  - **`engines`**

    `array`, default: `[]`

    **Items:**

    `string`

  - **`img_src`**

    `string`

  - **`parsed_url`**

    `array | null` — Parsed URL parts

  - **`positions`**

    `object`

  - **`priority`**

    `string`, possible values: `"", "high", "low"`, default: `""`

  - **`pubdate`**

    `string`

  - **`publishedDate`**

    `string | null`

  - **`score`**

    `number`, default: `0`

  - **`template`**

    `string`, default: `"default.html"`

  - **`thumbnail`**

    `string`

  - **`title`**

    `string`

  - **`url`**

    `string | null`, format: `uri`

  * **`attributes` (required)**

    `array`

    **Items:**

  * **`infobox` (required)**

    `string`

  * **`urls` (required)**

    `array`

    **Items:**

- **`number_of_results` (required)**

  `integer` — Average number of results reported by engines

- **`query` (required)**

  `string` — The search query

- **`results` (required)**

  `array` — List of search results (typed or legacy)

  **Items:**

  **Any of:**

  **All of:**

  - **`engine`**

    `string` — Engine providing the answer

  - **`parsed_url`**

    `array` — Parsed URL parts

    **Items:**

    `string`

  - **`url`**

    `string`, format: `uri` — A link related to this \*result\*

  * **`audio_src`**

    `string` — URL of an embedded \`\<audio controls>\`

  * **`author`**

    `string` — Author of the title

  * **`category`**

    `string` — Result category if available

  * **`content`**

    `string` — Extract or description of the result item

  * **`engines`**

    `array`, default: `[]` — In a merged results list, the names of the engines that found this result are listed in this field

    **Items:**

    `string`

  * **`iframe_src`**

    `string` — URL of an embedded \`\<iframe>\` / the frame is collapsible

  * **`img_src`**

    `string` — URL of a image that is displayed in the result item

  * **`length`**

    `number` — Playing duration in seconds

  * **`metadata`**

    `string` — Miscellaneous metadata

  * **`positions`**

    `array`, default: `[]` — Result ranks across engines

    **Items:**

    `integer`

  * **`priority`**

    `string`, possible values: `"", "high", "low"`, default: `""` — The priority can be set via :ref:\`hostnames plugin\`, for example

  * **`pubdate`**

    `string` — String representation of \`MainResult.publishedDate\`. Deprecated: it is still partially used in the templates, but will one day be completely eliminated.

  * **`publishedDate`**

    `string | null` — The date on which the object was published

  * **`score`**

    `number`, default: `0` — Relevance score

  * **`template`**

    `string`, default: `"default.html"` — Name of the template used to render the result

  * **`thumbnail`**

    `string` — URL of a thumbnail that is displayed in the result item

  * **`title`**

    `string` — Link title of the result item

  * **`views`**

    `string` — View count in humanized number format

  - **`category`**

    `string`

  - **`content`**

    `string`

  - **`engine`**

    `string`

  - **`engines`**

    `array`, default: `[]`

    **Items:**

    `string`

  - **`img_src`**

    `string`

  - **`parsed_url`**

    `array | null` — Parsed URL parts

  - **`positions`**

    `object`

  - **`priority`**

    `string`, possible values: `"", "high", "low"`, default: `""`

  - **`pubdate`**

    `string`

  - **`publishedDate`**

    `string | null`

  - **`score`**

    `number`, default: `0`

  - **`template`**

    `string`, default: `"default.html"`

  - **`thumbnail`**

    `string`

  - **`title`**

    `string`

  - **`url`**

    `string | null`, format: `uri`

- **`suggestions` (required)**

  `array` — Autocomplete suggestions if provided

  **Items:**

  `string`

- **`unresponsive_engines` (required)**

  `array` — List of unresponsive engines

  **Items:**

  `array`

**Example:**

```json
{
  "query": "",
  "number_of_results": 0,
  "results": [
    {
      "url": "",
      "engine": "",
      "parsed_url": [
        ""
      ],
      "template": "default.html",
      "title": "",
      "content": "",
      "img_src": "",
      "iframe_src": "",
      "audio_src": "",
      "thumbnail": "",
      "publishedDate": null,
      "pubdate": "",
      "length": 1,
      "views": "",
      "author": "",
      "metadata": "",
      "priority": "",
      "engines": [],
      "positions": [],
      "score": 0,
      "category": ""
    }
  ],
  "answers": [
    {
      "url": "",
      "engine": "",
      "parsed_url": [
        ""
      ]
    }
  ],
  "corrections": [
    ""
  ],
  "infoboxes": [
    {
      "url": null,
      "template": "default.html",
      "engine": "",
      "parsed_url": [
        ""
      ],
      "title": "",
      "content": "",
      "img_src": "",
      "thumbnail": "",
      "priority": "",
      "engines": [],
      "positions": [],
      "score": 0,
      "category": "",
      "publishedDate": null,
      "pubdate": "",
      "infobox": "",
      "urls": [
        {
          "propertyName*": ""
        }
      ],
      "attributes": [
        {
          "propertyName*": ""
        }
      ]
    }
  ],
  "suggestions": [
    ""
  ],
  "unresponsive_engines": [
    []
  ]
}
```

###### Content-Type: application/rss+xml

- **`channel` (required)**

  `object` — RSS channel metadata

  - **`description` (required)**

    `string` — Channel description

  - **`item` (required)**

    `array` — Entries contained in the channel

    **Items:**

    - **`guid` (required)**

      `string` — Stable entry identifier

    - **`link` (required)**

      `string`, format: `uri` — Entry link

    - **`title` (required)**

      `string` — Entry title

    - **`category`**

      `string` — Entry category

    - **`description`**

      `string` — Entry summary

    - **`pubDate`**

      `string` — Publication date (RFC 822)

  - **`link` (required)**

    `string`, format: `uri` — Channel link

  - **`title` (required)**

    `string` — Channel title

  - **`docs`**

    `string`, format: `uri` — Documentation URL for RSS

  - **`generator`**

    `string` — Feed generator identifier

  - **`language`**

    `string` — Language tag for the feed

  - **`lastBuildDate`**

    `string` — Last build timestamp (RFC 822)

- **`version`**

  `string`, possible values: `"2.0"` — RSS version attribute

**Example:**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<rss>
  <version>2.0</version>
  <channel>
    <title></title>
    <link></link>
    <description></description>
    <language></language>
    <generator></generator>
    <docs></docs>
    <lastBuildDate></lastBuildDate>
    <item>
      <title></title>
      <link></link>
      <guid></guid>
      <pubDate></pubDate>
      <description></description>
      <category></category>
    </item>
  </channel>
</rss>
```

###### Content-Type: application/csv

`string`, format: `binary`

**Example:**

```json
{}
```

## Schemas

### Result

- **Type:**`object`

Base class of all result types

- **`engine`**

  `string` — Engine providing the answer

- **`parsed_url`**

  `array` — Parsed URL parts

  **Items:**

  `string`

- **`url`**

  `string`, format: `uri` — A link related to this \*result\*

**Example:**

```json
{
  "url": "",
  "engine": "",
  "parsed_url": [
    ""
  ]
}
```

### MainResult

- **Type:**

Base class of all result types displayed in area main results

**Example:**

### LegacyResult

- **Type:**`object`

A wrapper around a legacy result item. The SearXNG core uses this class for untyped dictionaries / to be downward compatible. This class is needed until we have implemented an `Result` class for each result type and the old usages in the codebase have been fully migrated.

- **`category`**

  `string`

- **`content`**

  `string`

- **`engine`**

  `string`

- **`engines`**

  `array`, default: `[]`

  **Items:**

  `string`

- **`img_src`**

  `string`

- **`parsed_url`**

  `array | null` — Parsed URL parts

- **`positions`**

  `object`

- **`priority`**

  `string`, possible values: `"", "high", "low"`, default: `""`

- **`pubdate`**

  `string`

- **`publishedDate`**

  `string | null`

- **`score`**

  `number`, default: `0`

- **`template`**

  `string`, default: `"default.html"`

- **`thumbnail`**

  `string`

- **`title`**

  `string`

- **`url`**

  `string | null`, format: `uri`

**Example:**

```json
{
  "url": null,
  "template": "default.html",
  "engine": "",
  "parsed_url": [
    ""
  ],
  "title": "",
  "content": "",
  "img_src": "",
  "thumbnail": "",
  "priority": "",
  "engines": [],
  "positions": [],
  "score": 0,
  "category": "",
  "publishedDate": null,
  "pubdate": ""
}
```
